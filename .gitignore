# macOS
.DS_Store

# Xcode
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip

# Swift Package Manager
.build/
.swiftpm/
Packages/

# Engine test binaries
Tests/.build/

# Local signing team — see Signing.xcconfig
Local.xcconfig

# App Store media — regenerated by Tools/appstore_media.sh, see
# AppStore/screenshots.md. Around 150 MB a run, and stale the moment the app
# changes, so it is made on demand rather than carried in the repository.
AppStore/screenshots/
AppStore/screenshots-captioned/
AppStore/preview/
